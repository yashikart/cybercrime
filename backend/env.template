# Database Configuration
DATABASE_URL=sqlite:///./investigation.db

# Email Configuration (Brevo)
# SMTP values are optional and mainly for local/dev. On Render, prefer the HTTPS API.
MAIL_SERVER=smtp-relay.brevo.com
MAIL_PORT=587
MAIL_USERNAME=9fcd20001@smtp-brevo.com
MAIL_PASSWORD=YOUR_BREVO_SMTP_KEY_HERE
MAIL_FROM=blackholeinfiverse48@gmail.com
MAIL_FROM_NAME=Cybercrime Investigation System
MAIL_STARTTLS=true
MAIL_SSL_TLS=false
USE_CREDENTIALS=true
VALIDATE_CERTS=true

# Brevo HTTPS API key (recommended for Render). Use your xkeysib-... key here.
BREVO_API_KEY=YOUR_BREVO_HTTP_API_KEY_HERE

# Security
SECRET_KEY=your-secret-key-change-in-production-use-env-variable

# API Configuration
HOST=0.0.0.0
PORT=3000
DEBUG=True
